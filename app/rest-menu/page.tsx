import RestMenuPageClient from './rest-menu-client';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function RestMenuPage() {
  return <RestMenuPageClient />;
}

